/**
 * UNUSED — localStorage class booking prototype.
 * Only referenced by MemberSessionsBooking.tsx (also unused / not routed).
 * General and VIP class enrollment uses backend APIs. Safe to delete.
 */

/*
const BOOKINGS_KEY = 'roar_class_bookings'

export interface ClassBooking { ... }
export interface SlotRegistration { ... }

function readAll(): BookingsByMember { ... }
function writeAll(data: BookingsByMember): void { ... }
function memberKey(email: string): string { ... }
export function getMemberBookings(email: string): ClassBooking[] { ... }
export function isSlotBooked(email: string, slotId: string): boolean { ... }
export function bookClassSlot(...): ClassBooking[] { ... }
export function cancelClassBooking(...): ClassBooking[] { ... }
export function getRegistrationsForSlot(slotId: string): SlotRegistration[] { ... }
 */

export {}
